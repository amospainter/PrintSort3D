# Testing

Both packages use [Vitest](https://vitest.dev/). Run everything from the repo root:

```sh
npm test
```

Or per package:

```sh
npm test --prefix server
npm test --prefix client
```

Add `-- --watch` (e.g. `npm test --prefix server -- --watch`) to run in watch mode during development.

## Server tests (`server/src/*.test.ts`)

| File | Covers |
|---|---|
| `threeMf.test.ts` | `extractThreeMfData()` against on-the-fly zip fixtures (built with `adm-zip` in the test itself): Bambu plate thumbnail + filament/layer-height extraction, thumbnail-candidate fallback order, scalar vs. array config values, corrupt/non-zip files, invalid JSON in `project_settings.config`. `listThreeMfImages()`: returns every `Metadata/` image (not just the primary candidates), excludes non-`Metadata/` and non-image entries, empty list for no images or a corrupt file. `computePlateInfo()` (the single-parse merge): its `buildIndices`/`names` maps match the two standalone `computePlate*` functions, and names still resolve when `3D/3dmodel.model` is missing. Two regression tests reproduce the real "ADM-ZIP: Descriptor data is malformed" crash (a malformed zip entry's `getData()` throws) by mocking `adm-zip` so one named entry throws while the rest of the archive still decodes — see "Mocking `adm-zip`" below. |
| `dimensions.test.ts` | `computeDimensions()` per format: binary STL (a hand-built buffer with known triangle vertices), ASCII STL, OBJ (ignoring `vt`/`vn`/`vp` lines), and 3MF (vertices read from a fixture `.model` XML, including a union across multiple `<object>`s). A truncated binary STL (size mismatch, no `solid` prefix) returns `null` rather than regexing its bytes as ASCII. Regression test for real Bambu/OrcaSlicer exports: geometry split across `3D/Objects/object_N.model` part files with the root `3D/3dmodel.model` as just a manifest — an earlier version only read the first `.model` entry and silently returned `null` for every real Bambu file. Also: zero-triangle/no-vertex files, missing `.model` entry, corrupt/non-zip 3MF, unsupported extension, nonexistent file, and (same `adm-zip` mock as `threeMf.test.ts`) a malformed part file alongside a good one — all return `null`/skip cleanly rather than throwing. `dimensionsFromParts()`: bounding box across every part, `null` for empty parts. |
| `assets.test.ts` | `cacheThreeMfImages()`: converts each embedded image to a real WebP file under `ASSETS_DIR/<fileId>/` (asserts the `WEBP` magic bytes, not just that a file exists), skips undecodable images without throwing, empty array for a 3MF with no images. `deleteCachedImages()`: removes the directory tree, no-ops (doesn't throw) if it doesn't exist. `saveThumbnail()`/`thumbnailFilePath()`: writes/locates `thumbnail.png` under the same `ASSETS_DIR/<fileId>/`. `pruneOrphanAssets()`: deletes numeric-named directories absent from the live-id set, leaves live ones and non-numeric dirs. |
| `scanner.test.ts` | `runScan()` end-to-end against real files in a temp directory: adding new files, idempotent rescans, 3MF metadata/thumbnail extraction wired into a scan, embedded images cached as WebP during a scan, the baked render mesh + content/geometry hashes + archive entry counts written during a scan, a **server-rendered PNG thumbnail** produced for a plain STL from the baked mesh (and *not* rendered over a Bambu 3MF that has its own embedded plate image), dimensions computed and stored for new/changed files of any extension (asserted against hand-built STL/3MF fixtures with known geometry), `NULL` dimensions left alone for files with no parseable geometry, `slice_info.config` extraction into `slice_info_json` (and `null` when a 3MF has none), a whole root left un-flagged when its path goes unreachable, a symlinked/junctioned subdirectory being followed, and missing-file flagging/un-flagging as files are deleted and recreated. Scan-job coverage: `getScanProgress()` is idle → running/`processing` (polled mid-scan) → `done`; `runScan({ mode: 'reprocess-stale' })` re-runs processing on version-behind rows with no filesystem walk (and flags a row whose file vanished without a missing-flag sweep of others); `pendingReprocessCount()`. `scanner_version` backfill regression tests: a row with `scanner_version` reset to 0 (simulating a pre-existing catalog.db) gets its dimensions/3MF metadata/hashes/**missing thumbnail** recomputed on rescan *even with `mtime` unchanged*, and — the counterpart bug this guards against — a file with no parseable geometry is *not* reprocessed on every subsequent scan just because `dimension_x` stays `NULL`. `runScan()` is async (awaits WebP conversion + thumbnail rendering), so every call site in tests is `await`ed. |
| `thumbnail.test.ts` | `renderBakedThumbnail()` — the CPU software rasterizer: produces a 256×256 RGBA PNG from a baked cube (asserts PNG magic + `sharp` metadata), actually draws the model (a mix of opaque and transparent pixels, not a blank frame), renders indexed and non-indexed geometry of the same cube byte-identically, and returns `null` for empty parts or degenerate zero-size geometry. No GPU/WebGL involved. |
| `routes.test.ts` | Every `/api/*` endpoint via `createApp()` + Supertest: search/filter, `GET /api/files` pagination (page/pageSize slicing, past-the-end pages, pageSize clamped to the max), tag dedup+lowercasing, 404s, `POST /api/files/bulk-tags` (bulk add/remove across many files, 400 on empty `fileIds`), `GET`/`PUT /api/settings` for `defaultPlateSize`, `GET /api/raw/:id?download=1` serving the file as an attachment (`Content-Disposition`), `sliceInfo` serialized from `slice_info.config` plus `sort=printTime`/`dir=asc` ordering, `slicerMetadata` omitted from list responses but present on the detail route, `DELETE /api/files/:id` (409 for a present file, 200 + gone for a missing one) and `POST /api/files/purge-missing` (clears every missing file, reports the count), `GET /api/files/:id/thumbnail` serving the server-rendered PNG for a scanned model with geometry (PNG magic bytes) and 404 when none cached, `thumbnailUrl` left `null` for a no-geometry file, `/api/assets/:fileId/:filename` serving cached WebP images and rejecting non-webp/traversal-looking filenames, the `/api/raw/:id` path-traversal guard (a record's `relative_path` is tampered with directly in the DB to simulate an escape attempt), that `dimensions` is `null` vs. populated depending on whether the file has parseable geometry, that removing a root via `PUT /api/roots` deletes that root's files/thumbnails/cached-images/tag-links instead of leaving them orphaned, and `duplicate_count` staying consistent (via the scoped recompute) as a hash-mate is scanned in and then purged out. |
| `db.test.ts` | Writes a deliberately *old-schema* `catalog.db` (an early release's `files`/`tags` columns only) to disk, then imports `db.ts` so its migrations run against it: every `ALTER TABLE ... ADD COLUMN` in the guarded loop lands (including `slice_info_json`, `duplicate_count`), `tags.color` is added, existing rows survive with `scanner_version` and `duplicate_count` backfilled to 0, the legacy `thumbnails/<id>.png` files are relocated to `ASSETS_DIR/<id>/thumbnail.png` (old dir removed, `files.thumbnail_path` normalized), and the sort/hash/`duplicate_count` indexes are created. This is the "upgrade an existing user's database" path the other suites skip (they start from a fresh `CREATE TABLE`). |
| `fingerprint.test.ts` | `computeContentHash()` (byte-identical → same, different → different, null for a missing file) and `computeGeometryHash()` — order/translation invariance, cross-format (STL vs OBJ) and binary-vs-ASCII STL identity, `null` for no geometry. `computeGeometryHashFromParts()` matches `computeGeometryHash()` for the same vertices and keeps the invariances. |
| `scanPool.test.ts` | With `SCAN_WORKERS` unset, `extractArtifactsPooled()` runs inline and returns the same `FileArtifacts` as `extractArtifacts()` (dimensions, geometry hash, mesh/thumbnail written), and yields `null` geometry for a no-mesh file while still computing `contentHash`. |
| `archive.test.ts` | `listArchiveModelEntries()` (model files only, empty for none, no throw on a corrupt zip), `readArchiveEntry()`, and `readArchiveModelEntry()` — one archive open, rejects a non-model entry / missing entry / corrupt zip. |
| `meshBake.test.ts` | `bakeModel()` per format. 3MF: build-item transform folded into vertices + per-triangle paint slots, one part per `<build><item>` in document order, `paint` left `null` when nothing is painted, winding reversed for a negative-determinant transform, **scientific-notation coords with a negative exponent** (`1.5e-05` — regression for the NaN-vertex "spike to the plate corner" bug), multi-level `<component p:path>` graph resolved across part files with transforms composed, a malformed `.model` entry skipped while the rest still bakes, and the manifest found by its `<build>` section when it isn't named `3D/3dmodel.model`. STL/OBJ: binary + ASCII STL soup, OBJ polygon fan-triangulation with `v/vt/vn` face indices, `null` for a no-geometry file. |
| `paintColor.test.ts` | `decodePaintColor()`: simple (non-extended) leaf states, extended (>= 3) states from the two-nibble encoding, case-insensitivity, split-triangle collapse to the dominant leaf state, empty/malformed input → 0. `paintStateToFilamentIndex()` 1-based→0-based mapping and non-positive clamp. |
| `config.test.ts` | Env-derived roots (`PRINTSORT_ROOTS` / `PRINTSORT_MODELS_DIR`), and managed-root dedupe/merge/non-persistence. |
| `sliceInfo.test.ts` | `parseSliceInfoXml()`: single `<plate>` (print time / weight / filament / printer model / nozzle / supports), aggregation across multiple plates and filaments, `null` for a config with no `<plate>` blocks or non-XML, and tolerance of missing fields. |
| `security.test.ts` | `createApp()` + Supertest with each `PRINTSORT_*` env var: default (no auth, no CORS headers, CSRF guard refuses a foreign-`Origin` mutation, allows same-origin and no-`Origin`), `PRINTSORT_PASSWORD` (401 + `WWW-Authenticate`, right/wrong password), `PRINTSORT_READONLY` (GET ok, every mutation 403), `PRINTSORT_CORS_ORIGINS` (allowlisted origin echoed and allowed to mutate), `PRINTSORT_ROOTS_LOCKED` (403 `PUT /api/roots`). |

### Test isolation

`db.ts`, `config.ts`, and `paths.ts` read their file locations from `DB_PATH`, `CONFIG_PATH`, and `ASSETS_DIR` env vars (falling back to the normal on-disk locations when unset). Each test file:

1. Creates its own temp directory (`fs.mkdtempSync`).
2. Sets those env vars to point inside it.
3. **Dynamically imports** `db`, `config`, `scanner`, `assets`, and `app` (`await import('./db')`, etc.) — not static imports, since static imports are hoisted above the env-var assignments and would run against the real default paths.

Vitest isolates each test file's module graph by default, so this pattern doesn't leak env vars or module state between files — confirmed by running the full suite together, not just file-by-file.

`afterAll` closes the SQLite handle (`db.close()`) before deleting the temp directory; on Windows, deleting a directory containing a still-open DB file can throw, so that removal is wrapped in a `try/catch` (a cleanup nicety, not something a test asserts on).

### Mocking `adm-zip` to reproduce a malformed zip entry

`threeMf.test.ts` and `dimensions.test.ts` each reproduce the real "ADM-ZIP: Descriptor data is malformed" crash deterministically, without hand-crafting actually-corrupted zip bytes. The approach: `vi.mock('adm-zip', ...)` wraps the real module so that a specific entry name (set via a `vi.hoisted()`-declared mutable `corruptState.entryName`) has its `getData()` replaced with a function that throws.

The one non-obvious part: **`adm-zip` assigns `getEntries`/`getEntry` as own instance properties inside its constructor, not as prototype methods.** A first attempt at `class PatchedAdmZip extends RealAdmZip { getEntry(name) {...} }` silently never ran the override — the instance property set by the real constructor shadows a subclass's prototype method in JS property lookup. The fix wraps the *constructed instance* directly instead: `new RealAdmZip(path)`, then reassign `real.getEntries`/`real.getEntry` on that instance before returning it from a plain factory function. If you need to mock another `adm-zip` method the same way, mutate the instance post-construction, not the prototype.

## Client tests (`client/src/*.test.ts`)

| File | Covers |
|---|---|
| `loadModel.test.ts` | `frameObject()`: recenters an object's bounding box to the origin, scales camera distance with object size, keeps `near > 0` and `far > near`, doesn't throw on a zero-volume object. `loadModelAsObject3D()`: parses a real (inline ASCII) STL into a `THREE.Mesh`, is case-insensitive on the extension, rejects unsupported extensions. (Bounding-box *size* itself is no longer computed client-side — see `dimensions.test.ts` on the server.) |
| `api.test.ts` | The `api` fetch wrapper against a mocked `global.fetch` (`vi.stubGlobal`): query-string building (including omitting empty params), PATCH body shape, error messages on non-ok responses, and `rawFileUrl()`. |
| `tagColors.test.ts` | `resolveTagColor()` (explicit colour wins, deterministic preset fallback by name hash, empty string treated as unset, names spread across multiple presets), `tagChipStyle()` contrast pick (dark text on light bg / light on dark, 3-digit hex expansion, `#fff` fallback for an unparseable colour), and `tagColorMap()` name→colour lookup with `null` preserved. |

These run with `environment: 'node'` (see `client/vitest.config.ts`) since none of the current tests touch the DOM — they test pure logic (`three.js` math/parsing, `fetch` call shapes), not rendered components. If component-level tests are added later, switch that test's file (or the whole config) to `environment: 'jsdom'` (already installed as a dev dependency).

## What isn't covered (by design, so far)

- No React component rendering tests (`ModelViewer`, `ThemeToggle`, `TagInput`, page components) — these are wrappers around three.js/R3F, `localStorage`, and the `api` client, all tested directly. The Library page's multi-select (click / shift-range / rubber-band drag) and the Detail page's baked-vs-full-model + paint toggling are only exercised manually. Adding component tests would mean `@testing-library/react` + a jsdom WebGL shim.
- `theme.ts`'s 3-state resolution (`printsort3d-theme` localStorage key) has no unit test yet — pure logic, worth adding if the resolution rules change.
- No end-to-end/browser tests — the app was smoke-tested manually against real STL/OBJ/3MF files (including real Bambu Lab exports) during development; see the architecture doc for what that verified.
